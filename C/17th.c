#include <stdio.h>
#include <string.h>
int main()
{
    char str[100];
    long int len,i,j,f=0;
    gets(str);
    len=strlen(str);
    for(i=0;i<len;i++){
        for(j=i+1;j<len;j++){
            if(str[i]==str[j]){
                f=f;
            }
            else if(str[i]!=str[j]){
                if(str[j]==str[j+k])
            }

        }
    }
