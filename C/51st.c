#include <stdio.h>
int main()
{
    int
    scanf("%d\n",&n);
    scanf("%d",&a,&b,&c);
    for(i=0;i<n;i++){
        scanf("%d %d %d",&v,&d,&p);
        if(){
            w=p-a;
        }
        a--;
        if(w<0){
            f++;

        }
    }
}
